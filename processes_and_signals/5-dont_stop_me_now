#!/usr/bin/env bash
# Script to stop the 4-to_infinity_and_beyond process using kill

PIDS=$(pgrep -f "4-to_infinity_and_beyond")

if [ -z "$PIDS" ]; then
    echo "No 4-to_infinity_and_beyond process found."
else
    kill $PIDS
    echo "Stopped 4-to_infinity_and_beyond process."
fi

